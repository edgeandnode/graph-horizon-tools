query GetSubgraphById($id: ID!) {
  subgraph(id: $id) {
    id
    owner {
      id
    }
    currentVersion {
      subgraphDeployment {
        id
        ipfsHash
        signalledTokens
        queryFeesAmount
      }
    }
    createdAt
    updatedAt
    active
  }
}